# Signed deployment artifacts

This directory contains final N2con-signed NetLock RMM deployment scripts.

Filenames are opaque deployment IDs. Customer names and deployment-ID mappings are intentionally kept out of this public repository.

Do not publish unsigned drafts here. Do not modify a script after it has been signed.
